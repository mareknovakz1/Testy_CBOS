/**
 * @file DashboardApiService.ts
 * @author Marek Novák
 * @date 11.09.2025
 * @description
 * Tento soubor obsahuje DashboardApiService, specializovanou službu pro komunikaci
 * s dashboard endpointy API.
 * @classdesc
 * DashboardApiService dědí od BaseApiClient a přidává metody specifické pro
 * operace s dashboardem, jako je získání přehledových dat.
 * 
 */
import { BaseApiClient } from '../BaseApiClient';

export class DashboardApiService extends BaseApiClient {
    // Zde začneš přidávat metody specifické pro dashboard, například `getOverviewData`
}
